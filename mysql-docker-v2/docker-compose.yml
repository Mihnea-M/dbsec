services:
  mysql:
    image: mysql:8.0
    container_name: dbsec-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: test
    ports:
      - 3306:3306
    volumes:
      - ./data:/var/lib/mysql
